{
  "name": "explain-changes-mcp",
  "version": "1.0.1",
  "description": "MCP tool for AI to visualize code changes with inline annotations",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "explain-changes-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dev/server.mjs",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "explain-changes",
    "ai",
    "diff",
    "visualization"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
